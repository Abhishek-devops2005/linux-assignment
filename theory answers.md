1. What is the difference between mkdir and touch?
mdir is used to create directories
touch is used to create files

2. What is the difference between cp and mv?
cp is used to copy the file from one directory to another
mv is used to move the file from one directory to another.

3. What happens when you use cat > file.txt and the file already exists?
cat > file.txt is used to write into the file, and the previous content in the file is erased
4. What happens when you use cat >> file.txt and the file already exists?
cat>> file.txt will append the file with new text

5. What does Ctrl+D do when using cat > file.txt?
save the changes in the file after entering the text

6. What is the difference between cat and tac?
cat displays in the content of the file 
tac displays the content of the file in reverse order

7. What is the purpose of head? 
show/display 1st 5 line of the file

8. What is the purpose of tail?
show/display last 5 lines of the file

9. How do you display only the first 5 lines of a file?
head -5 filename.txt

10. How do you display only the last 5 lines of a file?
tail -5 filename.txt

11. What happens if you use mv to move a file to another directory?
File is moved from one directory to another directory.

12. Can cp copy a directory? If yes, what option is generally required?
use cp -r directory destination directory command can be used to copy the directory

13. where do you find system log files? which directory?
/var/logs
